# First run it for getting registered!
from notify_run import Notify
  
notify = Notify()
notify.register()

# Then comment the above code, and run the below code!
from notify_run import Notify
  
notify = Notify()
notify.send("Any Message you want to send")
